-- Storefront Red Team SQL workspace
-- Use this file to keep reusable aggregation or validation queries.

-- Example placeholder:
-- SELECT district_code, COUNT(*) AS row_count
-- FROM seoul_transaction_risk_scores
-- GROUP BY district_code
-- ORDER BY row_count DESC;
